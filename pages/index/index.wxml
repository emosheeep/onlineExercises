<!--index.wxml-->
<view class="container">
  <picker range="{{fileList}}" bindchange="changeQuestion">
    <view class="curQuestion">当前题库: <text>{{curListItem}}</text></view>
  </picker>
  <question class="question" name="{{curListItem}}" questions="{{questions}}"></question>
</view>
